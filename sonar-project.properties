#----- Default SonarQube server
sonar.host.url=http://sonarqube.as-crdbi-dev.com

#----- Default source code encoding
sonar.sourceEncoding=UTF-16
# must be unique in a given SonarQube instance
sonar.projectKey=Health-Partner-For-Knees-MS:Drupal
# this is the name displayed in the SonarQube UI
sonar.projectName=Health Partner for Knees MS - Drupal
sonar.projectVersion=1.1.6
sonar.projectBaseDir=/var/www/html/drupal


# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# Since SonarQube 4.2, this property is optional if sonar.modules is set.
# If not set, SonarQube starts looking for source code from the directory containing
# the sonar-project.properties file.
sonar.sources=modules/custom
sonar.exclusions=vendor/**
sonar.profile=Drupal
sonar.TestProp=Test